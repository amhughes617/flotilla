import React, { Component, } from 'react';
import { Link } from 'react-router';

/*will contain list of pages withing the app, renders in app with all routes
  will recieve action creators as props*/
export default (props) => {
  return (
    <div>This is a navbar!</div>
  );
}
