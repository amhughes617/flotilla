import React from 'react';

/*will contain a list of different map filter/overlay options(import single enum from actions for all the action types?)
  will recieve action creators as props*/
export default (props) => {
    return (
        <ul>
            <li>Public Landings</li>
        </ul>
    );
}
